local ContextMenu = {
	Cancel = require(script.Cancel),
	Checkbox = require(script.Checkbox),
	Color = require(script.Color),
	Confirm = require(script.Confirm),
	Container = require(script.Container),
	Dropdown = require(script.Dropdown),
	Header = require(script.Header),
	Item = require(script.Item),
}

return ContextMenu
